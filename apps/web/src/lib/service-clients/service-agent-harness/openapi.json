{
  "openapi": "3.1.0",
  "info": {
    "title": "agent_harness_service",
    "description": "",
    "termsOfService": "https://macro.com/terms",
    "license": {
      "name": ""
    },
    "version": "0.1.0"
  },
  "paths": {
    "/agent-models/load": {
      "post": {
        "tags": ["agent-models"],
        "summary": "Probe one provider's model catalog without creating an agent session.",
        "operationId": "load_agent_models_handler",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/LoadAgentModelsRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Fresh provider model catalog",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LoadAgentModelsResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid target"
          },
          "401": {
            "description": "Unauthenticated"
          },
          "403": {
            "description": "Harness is not visible to caller"
          },
          "409": {
            "description": "Macrod runtime is disconnected"
          },
          "502": {
            "description": "Provider probe failed"
          },
          "504": {
            "description": "Macrod probe timed out"
          }
        },
        "security": [
          {
            "bearerAuth": []
          }
        ]
      }
    },
    "/agent-repositories": {
      "get": {
        "tags": ["agent-repositories"],
        "summary": "List the GitHub repositories the caller can select for a coding session.",
        "operationId": "list_agent_repositories",
        "responses": {
          "200": {
            "description": "The caller's reachable repositories",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AgentRepositoriesResponse"
                }
              }
            }
          },
          "401": {
            "description": "Unauthenticated"
          },
          "502": {
            "description": "GitHub could not be asked which repositories the caller reaches"
          }
        },
        "security": [
          {
            "bearerAuth": []
          }
        ]
      }
    },
    "/agent-repositories/branches": {
      "get": {
        "tags": ["agent-repositories"],
        "summary": "List the branches on one repository the caller can start a session from.",
        "operationId": "list_agent_repository_branches",
        "parameters": [
          {
            "name": "repoUrl",
            "in": "query",
            "description": "Canonical https://github.com/owner/name URL",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The repository's branches",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AgentRepositoryBranchesResponse"
                }
              }
            }
          },
          "400": {
            "description": "The query did not name a GitHub repository"
          },
          "401": {
            "description": "Unauthenticated"
          },
          "403": {
            "description": "The caller cannot reach this repository"
          },
          "502": {
            "description": "GitHub could not be asked which branches the repository has"
          }
        },
        "security": [
          {
            "bearerAuth": []
          }
        ]
      }
    },
    "/agent-sandbox-size": {
      "get": {
        "tags": ["agent-sessions"],
        "summary": "Read the caller's default sandbox size for new `@coder` sessions.",
        "operationId": "get_agent_sandbox_size",
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SandboxSizeBody"
                }
              }
            }
          },
          "401": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "500": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          }
        }
      },
      "put": {
        "tags": ["agent-sessions"],
        "summary": "Set the caller's default sandbox size for the next `@coder` mention.",
        "operationId": "put_agent_sandbox_size",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SandboxSizeBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SandboxSizeBody"
                }
              }
            }
          },
          "401": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "500": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          }
        }
      }
    },
    "/agent-sessions": {
      "post": {
        "tags": ["agent-sessions"],
        "summary": "Open an agent session served by an external runtime.",
        "description": "Nothing here tells the runtime where to dial: one connection per bot\ncarries every session it runs, so a runtime that has already dialed serves\nthis session too, and one that has not dials the gateway its own\nconfiguration names. The triggering mention reaches the session as its\nfirst prompt through the control endpoint.",
        "operationId": "create_agent_session",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateAgentSessionRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CreateAgentSessionResponse"
                }
              }
            }
          },
          "401": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "403": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "404": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "422": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "500": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          }
        }
      }
    },
    "/agent-sessions/preview": {
      "post": {
        "tags": ["agent-sessions"],
        "summary": "Preview a batch of agent sessions for rendering chips.",
        "description": "No per-id access extractor: a chip has to render for a session the caller\ncannot open, so access is answered per id in the body rather than\nenforced on the request. The caller learns the fields a chip shows for\nsessions they may view, and only existence for the rest.",
        "operationId": "preview_agent_sessions",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PreviewAgentSessionsRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PreviewAgentSessionsResponse"
                }
              }
            }
          },
          "400": {
            "description": "more than the maximum number of session ids",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "401": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "500": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          }
        }
      }
    },
    "/agent-sessions/{session_id}": {
      "get": {
        "tags": ["agent-sessions"],
        "summary": "Get an agent session by id.",
        "operationId": "get_agent_session",
        "parameters": [
          {
            "name": "session_id",
            "in": "path",
            "description": "ID of the agent session",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AgentSessionResponse"
                }
              }
            }
          },
          "401": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "403": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "500": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          }
        }
      },
      "delete": {
        "tags": ["agent-sessions"],
        "summary": "Delete an agent session and its live resources.",
        "operationId": "delete_agent_session",
        "parameters": [
          {
            "name": "session_id",
            "in": "path",
            "description": "ID of the agent session",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "200": {
            "description": ""
          },
          "401": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "403": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "500": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          }
        }
      }
    },
    "/agent-sessions/{session_id}/changes": {
      "get": {
        "tags": ["agent-sessions"],
        "summary": "The latest captured changes of an agent session: the changed files with\nstatuses and line counts, and how the latest capture attempt went.",
        "operationId": "get_agent_session_changes",
        "parameters": [
          {
            "name": "session_id",
            "in": "path",
            "description": "ID of the agent session",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AgentSessionChangesResponse"
                }
              }
            }
          },
          "401": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "403": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "500": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          }
        }
      }
    },
    "/agent-sessions/{session_id}/changes/patch": {
      "get": {
        "tags": ["agent-sessions"],
        "summary": "The unified diff behind the session's latest changeset.",
        "operationId": "get_agent_session_changes_patch",
        "parameters": [
          {
            "name": "session_id",
            "in": "path",
            "description": "ID of the agent session",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AgentSessionChangesPatchResponse"
                }
              }
            }
          },
          "401": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "403": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "404": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "500": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          }
        }
      }
    },
    "/agent-sessions/{session_id}/changes/refresh": {
      "post": {
        "tags": ["agent-sessions"],
        "summary": "Capture the session's changes again now. Answers at once with the state\nas it stands; the capture runs on and viewers are told when it lands.",
        "operationId": "refresh_agent_session_changes",
        "parameters": [
          {
            "name": "session_id",
            "in": "path",
            "description": "ID of the agent session",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "202": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AgentSessionChangesResponse"
                }
              }
            }
          },
          "401": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "403": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "500": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          }
        }
      }
    },
    "/agent-sessions/{session_id}/control": {
      "post": {
        "tags": ["agent-sessions"],
        "summary": "Perform a control operation on a live agent session.",
        "description": "Edit access suffices: whoever can prompt the bot through its thread can\nprompt it here.\n\nA caller may name the action with `actionId`; the response echoes it.\nRe-posting an id the session still holds queued or in flight reports that\naction's status rather than accepting a duplicate.",
        "operationId": "control_agent_session",
        "parameters": [
          {
            "name": "session_id",
            "in": "path",
            "description": "ID of the agent session",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ControlRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Accepted. `sent` reached the runtime; `queued` waits for the running turn to end and can be edited or removed meanwhile.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ControlResponse"
                }
              }
            }
          },
          "401": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "403": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "422": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "500": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          }
        }
      }
    },
    "/agent-sessions/{session_id}/log": {
      "get": {
        "tags": ["agent-sessions"],
        "summary": "The raw protocol log of one agent session.",
        "description": "Served unfolded from the latest successful load initialization, or the\nbeginning when no load succeeded. Consumers stage load attempts so failed\nor interrupted replay does not become visible conversation content.\n\nAn unknown session is an error: the response has to name the session's\nagent, and a session that never existed has none to name.",
        "operationId": "get_agent_session_log",
        "parameters": [
          {
            "name": "session_id",
            "in": "path",
            "description": "ID of the agent session",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AgentSessionLogResponse"
                }
              }
            }
          },
          "401": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "403": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "500": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          }
        }
      }
    },
    "/agent-sessions/{session_id}/name": {
      "put": {
        "tags": ["agent-sessions"],
        "summary": "Rename an agent session.",
        "operationId": "rename_agent_session",
        "parameters": [
          {
            "name": "session_id",
            "in": "path",
            "description": "ID of the agent session",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RenameAgentSessionRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": ""
          },
          "400": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "401": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "403": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "500": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          }
        }
      }
    },
    "/agent-sessions/{session_id}/permissions": {
      "get": {
        "tags": ["agent-sessions"],
        "summary": "Read sharing settings for a session the caller can view.",
        "operationId": "get_agent_session_permissions",
        "parameters": [
          {
            "name": "session_id",
            "in": "path",
            "description": "ID of the agent session",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SharePermissionV2"
                }
              }
            }
          },
          "403": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "500": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          }
        }
      },
      "patch": {
        "tags": ["agent-sessions"],
        "summary": "Update link, channel, or team sharing after owner authorization.",
        "operationId": "update_agent_session_permissions",
        "parameters": [
          {
            "name": "session_id",
            "in": "path",
            "description": "ID of the agent session",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateSharePermissionRequestV2"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SharePermissionV2"
                }
              }
            }
          },
          "400": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "403": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "409": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "500": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          }
        }
      }
    },
    "/agent-sessions/{session_id}/queue": {
      "get": {
        "tags": ["agent-sessions"],
        "summary": "The actions waiting to dispatch in this session, oldest first.",
        "operationId": "get_agent_session_queue",
        "parameters": [
          {
            "name": "session_id",
            "in": "path",
            "description": "ID of the agent session",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AgentSessionQueueResponse"
                }
              }
            }
          },
          "401": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "403": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "500": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          }
        }
      }
    },
    "/agent-sessions/{session_id}/queue/{action_id}": {
      "put": {
        "tags": ["agent-sessions"],
        "summary": "Replace a queued prompt's text before it dispatches.",
        "operationId": "edit_queued_action",
        "parameters": [
          {
            "name": "session_id",
            "in": "path",
            "description": "ID of the agent session",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          },
          {
            "name": "action_id",
            "in": "path",
            "description": "ID the action was accepted under",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/EditQueuedActionRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": ""
          },
          "401": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "403": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "404": {
            "description": "Already dispatched or never queued",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "422": {
            "description": "The queued action carries no text",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "500": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          }
        }
      },
      "delete": {
        "tags": ["agent-sessions"],
        "summary": "Remove a queued action before it dispatches. There is no un-sending: an\naction that already went out answers 404.",
        "operationId": "remove_queued_action",
        "parameters": [
          {
            "name": "session_id",
            "in": "path",
            "description": "ID of the agent session",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          },
          {
            "name": "action_id",
            "in": "path",
            "description": "ID the action was accepted under",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "204": {
            "description": ""
          },
          "401": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "403": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "404": {
            "description": "Already dispatched or never queued",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "500": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          }
        }
      }
    },
    "/agent-sessions/{session_id}/sandbox-size": {
      "put": {
        "tags": ["agent-sessions"],
        "summary": "Resize this session's sandbox and remember the size as the owner's default.",
        "operationId": "put_agent_session_sandbox_size",
        "parameters": [
          {
            "name": "session_id",
            "in": "path",
            "description": "ID of the agent session",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SandboxSizeBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SandboxSizeBody"
                }
              }
            }
          },
          "401": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "403": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "500": {
            "description": "",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          }
        }
      }
    },
    "/claude-auth": {
      "get": {
        "tags": ["claude-auth"],
        "summary": "Read connection status for the authenticated user only.",
        "operationId": "status",
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/StatusResponse"
                }
              }
            }
          },
          "401": {
            "description": "Unauthenticated"
          }
        },
        "security": [
          {
            "bearerAuth": []
          }
        ]
      },
      "delete": {
        "tags": ["claude-auth"],
        "summary": "Forget only the authenticated user's grant and cancel pending consent.",
        "operationId": "disconnect",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/EmptyRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Disconnected"
          },
          "403": {
            "description": "Disabled"
          }
        },
        "security": [
          {
            "bearerAuth": []
          }
        ]
      }
    },
    "/claude-auth/complete": {
      "post": {
        "tags": ["claude-auth"],
        "summary": "Exchange one code; never return access or refresh tokens.",
        "operationId": "complete",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CompleteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Connected"
          },
          "400": {
            "description": "Invalid code"
          },
          "409": {
            "description": "Expired or replayed"
          },
          "502": {
            "description": "Provider failed"
          }
        },
        "security": [
          {
            "bearerAuth": []
          }
        ]
      }
    },
    "/claude-auth/start": {
      "post": {
        "tags": ["claude-auth"],
        "summary": "Create an expiring PKCE challenge for the authenticated user.",
        "operationId": "start",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/EmptyRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/StartResponse"
                }
              }
            }
          },
          "403": {
            "description": "Disabled"
          },
          "429": {
            "description": "Too many attempts"
          }
        },
        "security": [
          {
            "bearerAuth": []
          }
        ]
      }
    }
  },
  "components": {
    "schemas": {
      "AccessLevel": {
        "type": "string",
        "description": "Ordered from least to most access top -> bottom",
        "enum": ["view", "comment", "edit", "owner"]
      },
      "AgentAction": {
        "oneOf": [
          {
            "allOf": [
              {
                "$ref": "#/components/schemas/AgentPromptAction",
                "description": "Send the agent a prompt."
              },
              {
                "type": "object",
                "required": ["type"],
                "properties": {
                  "type": {
                    "type": "string",
                    "enum": ["prompt"]
                  }
                }
              }
            ],
            "description": "Send the agent a prompt."
          },
          {
            "allOf": [
              {
                "$ref": "#/components/schemas/AgentSetModelAction",
                "description": "Switch the model the agent runs on."
              },
              {
                "type": "object",
                "required": ["type"],
                "properties": {
                  "type": {
                    "type": "string",
                    "enum": ["setModel"]
                  }
                }
              }
            ],
            "description": "Switch the model the agent runs on."
          },
          {
            "type": "object",
            "description": "Compact the agent's current context.",
            "required": ["type"],
            "properties": {
              "type": {
                "type": "string",
                "enum": ["compact"]
              }
            }
          },
          {
            "type": "object",
            "description": "Interrupt whatever the agent is doing.",
            "required": ["type"],
            "properties": {
              "type": {
                "type": "string",
                "enum": ["stop"]
              }
            }
          },
          {
            "allOf": [
              {
                "$ref": "#/components/schemas/AgentRespondElicitationAction",
                "description": "Answer an `elicitation/create` the agent sent.\n\nThe one action whose wire form is a JSON-RPC *response* rather than a\nrequest or notification: the agent asked, we answer on its id. The\nminted [`AgentActionId`] therefore never reaches the wire for this\naction; the fold correlates on the agent's id instead."
              },
              {
                "type": "object",
                "required": ["type"],
                "properties": {
                  "type": {
                    "type": "string",
                    "enum": ["respondElicitation"]
                  }
                }
              }
            ],
            "description": "Answer an `elicitation/create` the agent sent.\n\nThe one action whose wire form is a JSON-RPC *response* rather than a\nrequest or notification: the agent asked, we answer on its id. The\nminted [`AgentActionId`] therefore never reaches the wire for this\naction; the fold correlates on the agent's id instead."
          },
          {
            "allOf": [
              {
                "$ref": "#/components/schemas/AgentPermissionAction",
                "description": "Answer a permission request the agent is waiting on."
              },
              {
                "type": "object",
                "required": ["type"],
                "properties": {
                  "type": {
                    "type": "string",
                    "enum": ["respondToPermission"]
                  }
                }
              }
            ],
            "description": "Answer a permission request the agent is waiting on."
          }
        ],
        "description": "One thing a caller wants an agent to do."
      },
      "AgentActionId": {
        "type": "string",
        "format": "uuid",
        "description": "Identifies one accepted [`AgentAction`] end to end: returned by the\ncontrol endpoint, written as the JSON-RPC request id on the action's wire\nframe, and read back off that frame as `request_id` on the folded message\nit derives.\n\nA v7 uuid, so ids sort by mint time. Minted by the server at accept time,\nor by a client that speculated the action and named it in the control\nrequest - either way the server is the only writer of runtime-bound\nframes, so a uuid-shaped request id remains the whole ownership test. The\nmachine's own handshake request ids (`agent_session:{session}:{n}`) are\nnot uuids and stay `None`."
      },
      "AgentModelDto": {
        "type": "object",
        "description": "One model picker option.",
        "required": ["id", "name"],
        "properties": {
          "description": {
            "type": ["string", "null"],
            "description": "Optional provider description."
          },
          "group": {
            "type": ["string", "null"],
            "description": "Optional group heading supplied by the provider."
          },
          "id": {
            "type": "string",
            "description": "Provider model id."
          },
          "name": {
            "type": "string",
            "description": "Display name."
          }
        }
      },
      "AgentModelsStatusDto": {
        "type": "string",
        "description": "Model-selection availability returned over HTTP.",
        "enum": ["available", "unsupported"]
      },
      "AgentPermissionAction": {
        "type": "object",
        "description": "A user's answer to the agent's `session/request_permission`.\n\nUnlike every other action this is not a new request but the reply to one\nthe agent made, so it carries the agent's own request id rather than\nreceiving a server-minted one.",
        "required": ["requestId", "answer"],
        "properties": {
          "answer": {
            "$ref": "#/components/schemas/PermissionAnswer",
            "description": "What the user decided."
          },
          "requestId": {
            "description": "The agent's JSON-RPC request id, echoed verbatim from the folded\npermission part. A string or a number on the wire; agents mint both,\nand `7` does not answer `\"7\"`."
          }
        }
      },
      "AgentPromptAction": {
        "type": "object",
        "description": "Ask the agent to work on something.",
        "required": ["prompt"],
        "properties": {
          "attachments": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PromptAttachment"
            },
            "description": "Files the prompt refers to, in the order the user attached them.\nDelivered after the text as one `resource_link` block each."
          },
          "prompt": {
            "type": "string",
            "description": "What to tell the agent."
          }
        }
      },
      "AgentRepositoriesResponse": {
        "type": "object",
        "description": "Response body for `GET /agent-repositories`.",
        "required": ["repositories"],
        "properties": {
          "repositories": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/AgentRepositoryDto"
            },
            "description": "Every repository the caller reaches through Macro's GitHub App, sorted\nby `owner/name`. Empty when the App is installed nowhere the caller\nhas a claim to."
          }
        }
      },
      "AgentRepositoryBranchesResponse": {
        "type": "object",
        "description": "Response body for `GET /agent-repositories/branches`.",
        "required": ["branches"],
        "properties": {
          "branches": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Branch names on the repository, in the order GitHub returned them.\nEmpty when the repository has no commits yet."
          }
        }
      },
      "AgentRepositoryDto": {
        "type": "object",
        "description": "One repository the caller can select for a coding session.",
        "required": ["url"],
        "properties": {
          "defaultBranch": {
            "type": ["string", "null"],
            "description": "The branch a clone checks out, and where a session starts when its\nrequest selects this repository without a `repoBranch`. Absent for a\nrepository with no commits."
          },
          "url": {
            "type": "string",
            "description": "The canonical `https://github.com/owner/name` url, in the form\n`POST /agent-sessions` accepts as `repoUrl`."
          }
        }
      },
      "AgentRespondElicitationAction": {
        "allOf": [
          {
            "$ref": "#/components/schemas/ElicitationAnswer",
            "description": "The decision."
          },
          {
            "type": "object",
            "required": ["requestId"],
            "properties": {
              "requestId": {
                "$ref": "#/components/schemas/ElicitationRequestId",
                "description": "The agent's `elicitation/create` request id - not an\n[`AgentActionId`], because the agent minted it."
              }
            }
          }
        ],
        "description": "Answer an elicitation the agent is waiting on."
      },
      "AgentSessionChangesPatchResponse": {
        "type": "object",
        "description": "Response body for `GET /agent-sessions/{session_id}/changes/patch`.\n\nClients deserialize this, so both derives are used.",
        "required": ["patch"],
        "properties": {
          "patch": {
            "type": "string",
            "description": "The git-style unified diff of the current changeset."
          }
        }
      },
      "AgentSessionChangesResponse": {
        "type": "object",
        "description": "Response body for `GET /agent-sessions/{session_id}/changes`.\n\nClients deserialize this, so both derives are used.",
        "required": ["capturing"],
        "properties": {
          "attempt": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/CaptureAttemptDto",
                "description": "The latest attempt, if any was made."
              }
            ]
          },
          "capturing": {
            "type": "boolean",
            "description": "A capture is running right now."
          },
          "changeset": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/ChangesetDto",
                "description": "The latest capture, if any succeeded."
              }
            ]
          }
        }
      },
      "AgentSessionLogEntryDto": {
        "allOf": [
          {
            "$ref": "#/components/schemas/LogFrameDto",
            "description": "The frame: `direction` and the protocol envelope under `content`.\n\nSerialized by [`Message`] itself rather than rebuilt field by field, so\nthe bytes on the wire are exactly what the fold's own log type reads\nback. [`LogFrameDto`] describes the two fields that produces."
          },
          {
            "type": "object",
            "required": ["id", "createdAt"],
            "properties": {
              "createdAt": {
                "type": "string",
                "format": "date-time",
                "description": "When the log recorded the frame.\n\nThe frame itself carries no time, so this comes from the log row. It is\nwhat a reader has to order these against anything else it is showing\nbeside them - the fold derives an order among the messages of one\nsession and nothing more."
              },
              "id": {
                "type": "string",
                "format": "uuid",
                "description": "Durable transport row identity; together with `createdAt`, its order cursor."
              },
              "userId": {
                "type": ["string", "null"],
                "description": "The user whose action produced the frame, absent when no user did.\n\nOnly prompts carry one, and only when the frame was attributed at the\ntime - a replayed or recorded session's are anonymous."
              }
            }
          }
        ],
        "description": "One entry of a session's protocol log.\n\nSerializes as `{\"userId\": ..., \"direction\": ..., \"content\": ...}` - the\nframe's own two fields, flattened in beside the attribution, which is the\nsame shape a recorded session's JSONL carries. A reader can deserialize the\n`direction`/`content` pair straight back into the fold's own log type\nrather than through a transport vocabulary of its own.\n\n`agentSessionId` is not repeated per entry: every entry in a response\nbelongs to the session named once at the top.\n\n`Deserialize` is for the wire-contract tests only - nothing server-side\ndecodes its own response type."
      },
      "AgentSessionLogResponse": {
        "type": "object",
        "description": "Response body for one session's raw protocol log.\n\nA wrapper rather than a bare array so that anything which is about the\nresponse rather than about a frame has somewhere to go later without\nbreaking every client.",
        "required": ["bot", "entries"],
        "properties": {
          "bot": {
            "$ref": "#/components/schemas/SessionBot",
            "description": "The agent whose messages the log derives.\n\nHere because a client renders those messages and cannot otherwise work\nout who sent them: the sender of an agent message is this session's\nbot, and nothing else names it."
          },
          "entries": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/AgentSessionLogEntryDto"
            },
            "description": "Effective history in ascending `(createdAt, id)` order.\nThe first row is the inclusive history boundary cursor: buffered rows\nbefore it are obsolete. Reconcile snapshot overlap by row ID, never content.\nAn empty history has no boundary or overlapping rows."
          }
        }
      },
      "AgentSessionPreviewData": {
        "type": "object",
        "description": "The fields a chip renders for a session the caller may view.\n\nClients deserialize this, so both derives are used.",
        "required": [
          "id",
          "name",
          "ownerId",
          "botId",
          "status",
          "createdAt",
          "modifiedAt"
        ],
        "properties": {
          "bot": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/SessionBot",
                "description": "Minimal identity of the session's bot, when available."
              }
            ]
          },
          "botId": {
            "type": "string",
            "format": "uuid",
            "description": "The bot running the agent."
          },
          "createdAt": {
            "type": "string",
            "format": "date-time",
            "description": "When the session was created."
          },
          "id": {
            "type": "string",
            "format": "uuid",
            "description": "The session id."
          },
          "modifiedAt": {
            "type": "string",
            "format": "date-time",
            "description": "When the session was last modified."
          },
          "name": {
            "type": "string",
            "description": "User-facing session name."
          },
          "ownerId": {
            "type": "string",
            "description": "The user who owns the session."
          },
          "status": {
            "$ref": "#/components/schemas/SessionStatusDto",
            "description": "The session's last known status."
          }
        }
      },
      "AgentSessionPreviewDto": {
        "oneOf": [
          {
            "allOf": [
              {
                "$ref": "#/components/schemas/AgentSessionPreviewData",
                "description": "The caller may view the session."
              },
              {
                "type": "object",
                "required": ["type"],
                "properties": {
                  "type": {
                    "type": "string",
                    "enum": ["access"]
                  }
                }
              }
            ],
            "description": "The caller may view the session."
          },
          {
            "allOf": [
              {
                "$ref": "#/components/schemas/WithAgentSessionId",
                "description": "The session exists but the caller holds no grant on it."
              },
              {
                "type": "object",
                "required": ["type"],
                "properties": {
                  "type": {
                    "type": "string",
                    "enum": ["no_access"]
                  }
                }
              }
            ],
            "description": "The session exists but the caller holds no grant on it."
          },
          {
            "allOf": [
              {
                "$ref": "#/components/schemas/WithAgentSessionId",
                "description": "No session with this id exists."
              },
              {
                "type": "object",
                "required": ["type"],
                "properties": {
                  "type": {
                    "type": "string",
                    "enum": ["does_not_exist"]
                  }
                }
              }
            ],
            "description": "No session with this id exists."
          }
        ],
        "description": "What one requested id resolved to, on the wire.\n\nTagged the same way the chat and document preview endpoints tag theirs\n(`type` in `access` / `no_access` / `does_not_exist`), so a client that\nrenders those chips can render this one with the same branch.\n\nClients deserialize this, so both derives are used."
      },
      "AgentSessionQueueResponse": {
        "type": "object",
        "description": "Response body for a session's queue: everything waiting, oldest first.\n\nA wrapper rather than a bare array so that anything which is about the\nresponse rather than about an entry has somewhere to go later without\nbreaking every client.",
        "required": ["entries"],
        "properties": {
          "entries": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/QueuedActionDto"
            },
            "description": "The waiting actions, in dispatch order."
          }
        }
      },
      "AgentSessionResponse": {
        "type": "object",
        "description": "Response body describing an agent session.\n\nClients deserialize this, so both derives are used.",
        "required": [
          "id",
          "name",
          "ownerId",
          "canEdit",
          "botId",
          "model",
          "harness",
          "workspace",
          "sandboxSize",
          "status",
          "createdAt",
          "modifiedAt"
        ],
        "properties": {
          "acpSessionId": {
            "type": ["string", "null"],
            "description": "The ACP session id, if one exists."
          },
          "botId": {
            "type": "string",
            "format": "uuid",
            "description": "The bot running the agent."
          },
          "canEdit": {
            "type": "boolean",
            "description": "Whether the caller may drive the session - prompt it, answer its\nquestions, stop it - rather than only watch. Edit access; the\ncreator owns the session, so a create response always says so."
          },
          "createdAt": {
            "type": "string",
            "format": "date-time",
            "description": "When the session was created."
          },
          "external": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/ExternalSessionResponse",
                "description": "The external provider serving this session, when one does. Absent for\nsandboxed sessions, so existing payloads are byte-identical."
              }
            ]
          },
          "harness": {
            "type": "string",
            "description": "Harness slug."
          },
          "id": {
            "type": "string",
            "format": "uuid",
            "description": "The session id."
          },
          "instructions": {
            "type": ["string", "null"],
            "description": "Instructions the session's runtime works under, when any were stated\nat creation. Absent otherwise, so existing payloads are unchanged."
          },
          "model": {
            "type": "string",
            "description": "Model slug."
          },
          "modifiedAt": {
            "type": "string",
            "format": "date-time",
            "description": "When the session was last modified."
          },
          "name": {
            "type": "string",
            "description": "User-facing session name."
          },
          "originatingMessageId": {
            "type": ["string", "null"],
            "format": "uuid",
            "description": "The exact message that invoked the bot, if any."
          },
          "ownerId": {
            "type": "string",
            "description": "The user who created and owns the session."
          },
          "pullRequestUrl": {
            "type": ["string", "null"],
            "description": "The session's linked pull request."
          },
          "repoUrl": {
            "type": ["string", "null"],
            "description": "The repository the session works with, when one was stated."
          },
          "sandboxSize": {
            "$ref": "#/components/schemas/SandboxSize",
            "description": "Compute tier of the managed sandbox."
          },
          "status": {
            "$ref": "#/components/schemas/SessionStatusDto",
            "description": "The session's status."
          },
          "threadChannelId": {
            "type": ["string", "null"],
            "format": "uuid",
            "description": "The channel `thread_id` lives in, when the session was spawned from a\nchannel thread. Derived from `thread_parent`."
          },
          "threadId": {
            "type": ["string", "null"],
            "format": "uuid",
            "description": "The root message of the thread the session was created from, if any."
          },
          "threadParent": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/MessageParent",
                "description": "The channel or document `thread_id` lives in, when the session was\nspawned from a thread."
              }
            ]
          },
          "workspace": {
            "type": "string",
            "description": "The directory the session's harness runs in on its runtime."
          }
        }
      },
      "AgentSetModelAction": {
        "type": "object",
        "description": "Ask the agent to run on a different model from here on.",
        "required": ["model"],
        "properties": {
          "model": {
            "type": "string",
            "description": "The model slug the agent should switch to."
          }
        }
      },
      "BotId": {
        "type": "string"
      },
      "CaptureAttemptDto": {
        "type": "object",
        "description": "The latest capture attempt.\n\nClients deserialize this, so both derives are used.",
        "required": ["startedAt"],
        "properties": {
          "error": {
            "type": ["string", "null"],
            "description": "Why it did not capture, in a sentence the user can read."
          },
          "finishedAt": {
            "type": ["string", "null"],
            "format": "date-time",
            "description": "When it ended; absent while it runs."
          },
          "outcome": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/CaptureOutcomeDto",
                "description": "How it ended; absent while it runs."
              }
            ]
          },
          "startedAt": {
            "type": "string",
            "format": "date-time",
            "description": "When it started."
          }
        }
      },
      "CaptureOutcomeDto": {
        "type": "string",
        "description": "How the latest capture attempt ended, on the wire.",
        "enum": ["captured", "not_ready", "failed"]
      },
      "ChangedFileDto": {
        "type": "object",
        "description": "One changed file.\n\nClients deserialize this, so both derives are used.",
        "required": [
          "path",
          "kind",
          "additions",
          "deletions",
          "binary",
          "patchOmitted"
        ],
        "properties": {
          "additions": {
            "type": "integer",
            "format": "int32",
            "description": "Lines added.",
            "minimum": 0
          },
          "binary": {
            "type": "boolean",
            "description": "The diff carries no text for this file."
          },
          "deletions": {
            "type": "integer",
            "format": "int32",
            "description": "Lines removed.",
            "minimum": 0
          },
          "kind": {
            "$ref": "#/components/schemas/FileChangeKindDto",
            "description": "What happened to the file."
          },
          "patchOmitted": {
            "type": "boolean",
            "description": "The file's hunks were left out of the patch to fit the size budget."
          },
          "path": {
            "type": "string",
            "description": "The file's path after the change, or before it for a deletion."
          },
          "previousPath": {
            "type": ["string", "null"],
            "description": "Where a renamed file came from."
          }
        }
      },
      "ChangesetDto": {
        "type": "object",
        "description": "One capture of a session's changes.\n\nClients deserialize this, so both derives are used.",
        "required": [
          "id",
          "source",
          "base",
          "head",
          "files",
          "additions",
          "deletions",
          "patchBytes",
          "truncated",
          "capturedAt"
        ],
        "properties": {
          "additions": {
            "type": "integer",
            "format": "int32",
            "description": "Lines added across all files.",
            "minimum": 0
          },
          "base": {
            "$ref": "#/components/schemas/GitRefDto",
            "description": "The side the work started from."
          },
          "capturedAt": {
            "type": "string",
            "format": "date-time",
            "description": "When the diff was taken."
          },
          "deletions": {
            "type": "integer",
            "format": "int32",
            "description": "Lines removed across all files.",
            "minimum": 0
          },
          "files": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ChangedFileDto"
            },
            "description": "Every changed file, in patch order."
          },
          "head": {
            "$ref": "#/components/schemas/GitRefDto",
            "description": "The side carrying the work."
          },
          "id": {
            "type": "string",
            "format": "uuid",
            "description": "The capture's id; changes with every capture."
          },
          "patchBytes": {
            "type": "integer",
            "format": "int64",
            "description": "Size of the patch `GET .../changes/patch` serves; zero when nothing\nchanged.",
            "minimum": 0
          },
          "repository": {
            "type": ["string", "null"],
            "description": "`https://github.com/owner/name`, when known."
          },
          "source": {
            "$ref": "#/components/schemas/ChangesetSourceDto",
            "description": "Where the diff was read from."
          },
          "truncated": {
            "type": "boolean",
            "description": "Some files' hunks were left out of the patch."
          }
        }
      },
      "ChangesetSourceDto": {
        "type": "string",
        "description": "The source of the captured diff, on the wire.",
        "enum": ["github_pull_request"]
      },
      "ChannelSharePermission": {
        "type": "object",
        "description": "The channel share permission",
        "required": ["channel_id", "access_level"],
        "properties": {
          "access_level": {
            "$ref": "#/components/schemas/AccessLevel",
            "description": "The access level for the channel"
          },
          "channel_id": {
            "type": "string",
            "description": "The channel id"
          }
        }
      },
      "CompleteRequest": {
        "type": "object",
        "description": "One-time manual code. Deliberately does not implement Debug.",
        "required": ["attemptId", "code"],
        "properties": {
          "attemptId": {
            "type": "string",
            "description": "Server-issued handle; never an owner selected by the caller."
          },
          "code": {
            "type": "string",
            "description": "Claude's complete code#state string, not an access token."
          }
        },
        "additionalProperties": false
      },
      "ControlRequest": {
        "allOf": [
          {
            "$ref": "#/components/schemas/AgentAction",
            "description": "The operation to perform."
          },
          {
            "type": "object",
            "properties": {
              "actionId": {
                "oneOf": [
                  {
                    "type": "null"
                  },
                  {
                    "$ref": "#/components/schemas/AgentActionId",
                    "description": "The id the caller already speculated this action under, when it has\none. Adopted as the accepted id, so a client that renders the action\noptimistically promotes that entry in place instead of retracting it\nand re-speculating under a server id. Re-sending the same id is not a\nsecond action: see [`ControlResponse`].\n\nNamed rather than flattened so it cannot collide with the action's own\nfields, which are tagged under `type`."
                  }
                ]
              }
            }
          }
        ],
        "description": "Request body for a control operation on a live session.\n\nA wrapper around the operation rather than the bare enum so that fields\nwhich are about the request rather than the operation have somewhere to go.\nThe acting user is deliberately not one of them: it comes from the caller's\ncredentials, so that a caller cannot attribute an operation to someone else.\n\nClients serialize this, so both derives are used."
      },
      "ControlResponse": {
        "type": "object",
        "description": "Response body for a control operation.\n\nClients deserialize this, so both derives are used.",
        "required": ["actionId", "status"],
        "properties": {
          "actionId": {
            "$ref": "#/components/schemas/AgentActionId",
            "description": "Matches `requestId` on the folded message this action derives once it\ndispatches, and names the queue entry until then. The caller's own\n`actionId` when it supplied one; a freshly minted id otherwise."
          },
          "status": {
            "$ref": "#/components/schemas/ControlStatusDto",
            "description": "Whether the action went out or waits in the queue."
          }
        }
      },
      "ControlStatusDto": {
        "type": "string",
        "description": "What accepting a control operation did with it, on the wire.",
        "enum": ["sent", "queued"]
      },
      "CreateAgentSessionRequest": {
        "type": "object",
        "description": "Request body for `POST /agent-sessions`.\n\nCarries two shapes, told apart by `workspace`. Naming one asks for an\nexternal session: the runtime is the bot operator's, so the caller has to\nsay which bot and which directory, and must own that bot. Omitting it asks\nfor a managed session, whose runtime this deployment provisions. A managed\nrequest may select an authorized persisted persona with `botId`; omitting\nit uses the deployment's default coding persona. Fields describing someone\nelse's runtime must still be omitted rather than quietly ignored. Mixing\nthe two shapes is refused rather than guessed at.\n\nClients serialize this, so both derives are used.",
        "properties": {
          "botId": {
            "type": ["string", "null"],
            "format": "uuid",
            "description": "Bot the session runs for. On a managed request this optionally selects\na persisted persona the user owns, may use through team membership, or\ncan `@` mention in a shared channel; omitting it uses the deployment's\ndefault coding persona. On an external request, bot callers may omit it\n(their own identity is used) and must not name another bot; user callers\nmust supply a bot they own."
          },
          "id": {
            "type": ["string", "null"],
            "format": "uuid",
            "description": "Id to create the session under, minted by the caller. Lets a surface\nopen on the session's final id - URL, history row, references - the\nmoment the user acts, rather than after this request answers (which\nfor a managed sandbox can take a while). Omitted, the service mints\none. Managed sessions only. Answers 409 if a session already holds\nthe id."
          },
          "instructions": {
            "type": ["string", "null"],
            "description": "Instructions the session's runtime works under, for its whole life.\n\nRecorded on the session whichever runtime serves it. Only the\nin-process one acts on them today; `agent_harness`'s `AgentKind`\nrecords what each of the others will need to."
          },
          "model": {
            "type": ["string", "null"],
            "description": "Model the managed session runs on, overriding the persona's. Managed\nsessions only: an external runtime picks its own.\n\nThe session's model from the moment it exists, which is what a caller\nchoosing one before the first prompt means. Selecting a model *during*\na session is a control action instead, and reads as one in its\ntranscript."
          },
          "owner": {
            "type": ["string", "null"],
            "description": "The user who owns the session. Ignored for user callers, who always\nown their own sessions, and for harness callers, whose verified acting\nuser (owner or confirmed team member) owns the session instead;\nrequired for bot callers without verified acting-user claims.\n\nFor bot callers this is a claim, not a verified fact: it is scoped to\nthe bot's own sessions, but the named user owns the session on the\nbot's say-so."
          },
          "prompt": {
            "type": ["string", "null"],
            "description": "First prompt to deliver once the session is running. Managed sessions\nonly - an external runtime sends its own first prompt through the\ncontrol endpoint. Omitted, the session opens idle."
          },
          "repoBranch": {
            "type": ["string", "null"],
            "description": "Starting branch for a managed coding session's selected repository.\nOmitted, the session starts on the repository's default branch."
          },
          "repoUrl": {
            "type": ["string", "null"],
            "description": "Explicit GitHub repository for a managed Cursor session, as one of the\nurls `GET /agent-repositories` lists for the caller. Access is checked\nfor the session owner. For external sessions this is informational:\ncloning it is the runtime operator's job."
          },
          "thread": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/CreateSessionThread",
                "description": "The thread whose mention triggered the session, when one did.\nLinkage only - the mention's text is delivered by the runtime as the\nfirst prompt through the control endpoint, never through here."
              }
            ]
          },
          "workspace": {
            "type": ["string", "null"],
            "description": "Absolute directory the bot's harness runs in on its runtime. Present\nfor an external session, absent for a managed one, which runs in the\npath baked into its image."
          }
        }
      },
      "CreateAgentSessionResponse": {
        "type": "object",
        "description": "Response body for `POST /agent-sessions`.\n\nClients deserialize this, so both derives are used.",
        "required": ["session"],
        "properties": {
          "session": {
            "$ref": "#/components/schemas/AgentSessionResponse",
            "description": "The created session."
          }
        }
      },
      "CreateSessionThread": {
        "type": "object",
        "description": "The triggering mention on a create request.\n\nClients serialize this, so both derives are used.",
        "required": ["messageId"],
        "properties": {
          "channelId": {
            "type": ["string", "null"],
            "format": "uuid",
            "description": "Channel the mentioning message was posted in. Runtimes built before\nmessage parents send this instead of `parent`."
          },
          "content": {
            "type": "string",
            "description": "The mention's text, quoted in the session's announcement."
          },
          "messageId": {
            "type": "string",
            "format": "uuid",
            "description": "The mentioning message."
          },
          "parent": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/MessageParent",
                "description": "Entity the mentioning message was posted in."
              }
            ]
          },
          "threadId": {
            "type": ["string", "null"],
            "format": "uuid",
            "description": "Thread the session belongs to; defaults to the message itself, which\nis how a top-level mention roots its own thread."
          }
        }
      },
      "DocumentId": {
        "type": "string",
        "description": "A validated document identifier. Historical document ids need not be UUIDs."
      },
      "EditQueuedActionRequest": {
        "type": "object",
        "description": "Request body for editing a queued prompt.",
        "required": ["prompt"],
        "properties": {
          "prompt": {
            "type": "string",
            "description": "The new raw prompt text, replacing the old wholesale. Never blank: a\nprompt with nothing to say is a removal, and there is an endpoint for\nthat.",
            "minLength": 1
          }
        }
      },
      "ElicitationAnswer": {
        "oneOf": [
          {
            "type": "object",
            "description": "The user submitted the form, or consented to open the URL.",
            "required": ["action"],
            "properties": {
              "action": {
                "type": "string",
                "enum": ["accept"]
              },
              "content": {
                "type": ["object", "null"],
                "description": "Form: the submitted values keyed by property. URL: omitted.",
                "additionalProperties": {
                  "$ref": "#/components/schemas/ElicitationContentValue"
                },
                "propertyNames": {
                  "type": "string"
                }
              }
            }
          },
          {
            "type": "object",
            "description": "The user explicitly said no.",
            "required": ["action"],
            "properties": {
              "action": {
                "type": "string",
                "enum": ["decline"]
              }
            }
          },
          {
            "type": "object",
            "description": "The user dismissed the request without choosing.",
            "required": ["action"],
            "properties": {
              "action": {
                "type": "string",
                "enum": ["cancel"]
              }
            }
          }
        ],
        "description": "What the user decided about an elicitation. Mirrors ACP's three actions;\nthere is no `Other` because we never originate an action we do not know."
      },
      "ElicitationContentValue": {
        "oneOf": [
          {
            "type": "string",
            "description": "A string, and the shape a whole draft rides in as JSON text."
          },
          {
            "type": "boolean",
            "description": "A yes/no."
          },
          {
            "type": "integer",
            "format": "int64",
            "description": "A whole number."
          },
          {
            "type": "number",
            "format": "double",
            "description": "A number that is not whole."
          },
          {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "A multi-select's chosen values."
          }
        ],
        "description": "A value ACP accepts in an elicitation answer.\n\nMirrors ACP's `ElicitationContentValue` so that the contract a caller\nanswers against is the closed union ACP will accept, rather than arbitrary\nJSON narrowed on the way out. An object, a null, or a mixed array is\nrefused when the request is deserialized - where the caller learns of it -\ninstead of at send time, when the elicitation slot has already been\nreleased."
      },
      "ElicitationRequestId": {
        "oneOf": [
          {
            "type": "integer",
            "format": "int64",
            "description": "A numeric JSON-RPC id. Specta refuses `i64` (it does not fit a JS\nnumber); agents count their requests from zero, so `i32` is the\nhonest TypeScript face."
          },
          {
            "type": "string",
            "description": "A string JSON-RPC id."
          }
        ],
        "description": "The JSON-RPC id of an agent's `elicitation/create` request, carried whole\nso the answer echoes exactly what the agent sent.\n\nAgents pick these, not us: Claude Code counts from `0`, others use\nstrings. `null` is not a legal id for a request that expects a response,\nso it is not representable here."
      },
      "EmptyRequest": {
        "type": "object",
        "description": "A JSON body is required on writes, including start/disconnect (no form-based CSRF).",
        "additionalProperties": false
      },
      "ExternalSessionResponse": {
        "type": "object",
        "description": "The provider-side identity of an externally-served session.",
        "required": ["provider"],
        "properties": {
          "name": {
            "type": ["string", "null"],
            "description": "The provider's display name for the agent, when it reported one."
          },
          "provider": {
            "type": "string",
            "description": "Which provider serves the session, e.g. `cursor`."
          },
          "url": {
            "type": ["string", "null"],
            "description": "The agent's page on the provider's site, for a client to link out to."
          }
        }
      },
      "FileChangeKindDto": {
        "type": "string",
        "description": "What happened to a file, on the wire.",
        "enum": ["added", "modified", "deleted", "renamed"]
      },
      "GitRefDto": {
        "type": "object",
        "description": "One end of the compared range.",
        "properties": {
          "name": {
            "type": ["string", "null"],
            "description": "The branch name, when known."
          },
          "sha": {
            "type": ["string", "null"],
            "description": "The commit, when known."
          }
        }
      },
      "LinkShare": {
        "type": "string",
        "description": "Defines who can access an item through its share link.",
        "enum": ["PUBLIC", "TEAM"]
      },
      "LoadAgentModelsRequest": {
        "type": "object",
        "description": "HTTP request selecting one provider to probe.",
        "required": ["harness"],
        "properties": {
          "harness": {
            "$ref": "#/components/schemas/ModelHarnessDto",
            "description": "Provider to probe."
          },
          "harnessId": {
            "type": ["string", "null"],
            "format": "uuid",
            "description": "Required for macrod and forbidden for other targets."
          }
        }
      },
      "LoadAgentModelsResponse": {
        "type": "object",
        "description": "Successful model-discovery response.",
        "required": ["status", "models"],
        "properties": {
          "currentModel": {
            "type": ["string", "null"],
            "description": "Current provider model, if model selection is available."
          },
          "models": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/AgentModelDto"
            },
            "description": "Ordered model catalog."
          },
          "status": {
            "$ref": "#/components/schemas/AgentModelsStatusDto",
            "description": "Model-selection availability."
          }
        }
      },
      "LogDirectionDto": {
        "type": "string",
        "description": "Which way a logged frame travelled, mirroring [`Message`]'s discriminant.",
        "enum": ["to_server", "to_runtime"]
      },
      "LogFrameDto": {
        "type": "object",
        "description": "The two fields [`AgentSessionLogEntryDto`] flattens in.\n\nSchema only. Nothing constructs one: the entry serializes through\n[`Message`], and this exists so the generated clients see `direction` and\n`content` as named fields instead of an open map. A hand-built copy could\ndrift from the fold's wire format, and the point of the endpoint is that it\ncannot - so this describes that format without being able to produce it.",
        "required": ["direction", "content"],
        "properties": {
          "content": {
            "type": "object",
            "description": "The protocol envelope, verbatim. Opaque here: it is Agent Runtime\nProtocol, whose shape belongs to the fold rather than this endpoint."
          },
          "direction": {
            "$ref": "#/components/schemas/LogDirectionDto",
            "description": "Which way the frame travelled."
          }
        }
      },
      "MessageParent": {
        "oneOf": [
          {
            "type": "object",
            "description": "A channel, including direct messages.",
            "required": ["id", "type"],
            "properties": {
              "id": {
                "type": "string",
                "format": "uuid",
                "description": "A channel, including direct messages."
              },
              "type": {
                "type": "string",
                "enum": ["channel"]
              }
            }
          },
          {
            "type": "object",
            "description": "A document, including tasks and PDFs.",
            "required": ["id", "type"],
            "properties": {
              "id": {
                "$ref": "#/components/schemas/DocumentId",
                "description": "A document, including tasks and PDFs."
              },
              "type": {
                "type": "string",
                "enum": ["document"]
              }
            }
          }
        ],
        "description": "The entity whose permissions and lifecycle govern a message."
      },
      "ModelHarnessDto": {
        "type": "string",
        "description": "Harness names accepted by the model discovery endpoint.",
        "enum": ["in-memory", "cursor", "claude-cloud", "macrod"]
      },
      "PermissionAnswer": {
        "oneOf": [
          {
            "type": "object",
            "description": "One of the options the agent offered was chosen.",
            "required": ["optionId", "kind"],
            "properties": {
              "kind": {
                "type": "string",
                "enum": ["selected"]
              },
              "optionId": {
                "type": "string",
                "description": "The chosen option's id, as the agent listed it."
              }
            }
          },
          {
            "type": "object",
            "description": "The request was dismissed without choosing.",
            "required": ["kind"],
            "properties": {
              "kind": {
                "type": "string",
                "enum": ["cancelled"]
              }
            }
          }
        ],
        "description": "The decision carried by an [`AgentPermissionAction`]."
      },
      "PreviewAgentSessionsRequest": {
        "type": "object",
        "description": "Request body for `POST /agent-sessions/preview`.\n\nClients serialize this, so both derives are used.",
        "required": ["sessionIds"],
        "properties": {
          "sessionIds": {
            "type": "array",
            "items": {
              "type": "string",
              "format": "uuid"
            },
            "description": "The sessions to preview. Duplicates are collapsed server-side; at most\n[`MAX_PREVIEW_SESSION_IDS`](crate::domain::model::MAX_PREVIEW_SESSION_IDS)\ndistinct ids per request."
          }
        }
      },
      "PreviewAgentSessionsResponse": {
        "type": "object",
        "description": "Response body for `POST /agent-sessions/preview`: one entry per distinct\nrequested id, in no particular order.\n\nClients deserialize this, so both derives are used.",
        "required": ["previews"],
        "properties": {
          "previews": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/AgentSessionPreviewDto"
            },
            "description": "What the caller may see of each requested session."
          }
        }
      },
      "PromptAttachment": {
        "type": "object",
        "description": "A file the prompt refers to, by where the agent can fetch it.\n\nMirrors ACP's `resource_link` content block, which every agent must\naccept: bytes never ride the prompt, only a URI (a static file service URL\nin practice) with enough metadata for a client to render a chip and for\nthe agent to decide whether to fetch it. Keeping the wire shape ACP-native\nmeans the harness translates without resolving anything, and the fold\nreads the same fields back off the logged frame.",
        "required": ["uri", "name"],
        "properties": {
          "mimeType": {
            "type": ["string", "null"],
            "description": "The file's media type, when known."
          },
          "name": {
            "type": "string",
            "description": "Display name, typically the original file name."
          },
          "size": {
            "type": ["integer", "null"],
            "format": "int64",
            "description": "Size in bytes, when known."
          },
          "uri": {
            "type": "string",
            "description": "Where the agent can fetch the file."
          }
        }
      },
      "QueuedActionDto": {
        "type": "object",
        "description": "One action waiting in a session's queue.\n\nClients deserialize this, so both derives are used.",
        "required": ["actionId", "kind", "createdAt"],
        "properties": {
          "actionId": {
            "$ref": "#/components/schemas/AgentActionId",
            "description": "The id the action was accepted under."
          },
          "actorUserId": {
            "type": ["string", "null"],
            "description": "The user who queued it, absent when a bot acted on nobody's behalf."
          },
          "attachments": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PromptAttachment"
            },
            "description": "Files the prompt refers to, for prompts only. Kept through an edit,\nwhich replaces the text alone."
          },
          "createdAt": {
            "type": "string",
            "format": "date-time",
            "description": "When it was accepted."
          },
          "kind": {
            "type": "string",
            "description": "What kind of action waits - `prompt` or `compact`; only\nturn-occupying actions are ever queued."
          },
          "prompt": {
            "type": ["string", "null"],
            "description": "The prompt's raw text, present for prompts only. What an edit\nreplaces."
          }
        }
      },
      "RenameAgentSessionRequest": {
        "type": "object",
        "description": "Request body for renaming an agent session.",
        "required": ["name"],
        "properties": {
          "name": {
            "type": "string",
            "description": "New user-facing name. Leading and trailing whitespace is discarded."
          }
        }
      },
      "SandboxSize": {
        "type": "string",
        "description": "Named compute tier for a managed sandbox.\n\nThe API and database store the name. CPU, RAM, and disk live in\n`crates/agent_harness/sandbox_sizes.json`.",
        "enum": ["small", "default", "large"]
      },
      "SandboxSizeBody": {
        "type": "object",
        "description": "Request or response body for a named sandbox size.",
        "required": ["size"],
        "properties": {
          "size": {
            "$ref": "#/components/schemas/SandboxSize",
            "description": "Named compute tier."
          }
        }
      },
      "SessionBot": {
        "type": "object",
        "description": "The agent behind a session, as much of it as rendering a message needs.",
        "required": ["id", "name", "handle"],
        "properties": {
          "avatarUrl": {
            "type": ["string", "null"],
            "description": "Avatar, when it has one."
          },
          "handle": {
            "type": "string",
            "description": "Stable `@` handle, without a leading `@`."
          },
          "id": {
            "$ref": "#/components/schemas/BotId",
            "description": "The bot's id. A message it sent has `\"bot|{id}\"` as its sender."
          },
          "name": {
            "type": "string",
            "description": "Display name."
          }
        }
      },
      "SessionStatusDto": {
        "oneOf": [
          {
            "type": "object",
            "description": "No status updates received.",
            "required": ["kind"],
            "properties": {
              "kind": {
                "type": "string",
                "enum": ["no_messages"]
              }
            }
          },
          {
            "type": "object",
            "description": "The last system event received from the runtime.",
            "required": ["event", "kind"],
            "properties": {
              "event": {
                "type": "string",
                "description": "The wire name of the system event, e.g. `acp_ready`."
              },
              "kind": {
                "type": "string",
                "enum": ["event"]
              }
            }
          },
          {
            "type": "object",
            "description": "The session disconnected without sending a closed event.",
            "required": ["kind"],
            "properties": {
              "kind": {
                "type": "string",
                "enum": ["disconnected"]
              }
            }
          }
        ],
        "description": "Transport representation of a session's status, mirroring\n[`SessionStatus`]."
      },
      "SharePermissionV2": {
        "type": "object",
        "required": ["id", "owner"],
        "properties": {
          "channelSharePermissions": {
            "type": ["array", "null"],
            "items": {
              "$ref": "#/components/schemas/ChannelSharePermission"
            },
            "description": "The channel share permissions for the item"
          },
          "id": {
            "type": "string",
            "description": "The share permission id"
          },
          "linkShare": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/LinkShare",
                "description": "Who can access the item through its share link"
              }
            ]
          },
          "linkShareAccessLevel": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/AccessLevel",
                "description": "The level of access granted through the share link"
              }
            ]
          },
          "owner": {
            "type": "string",
            "description": "The owner of the item"
          },
          "teamShareAccessLevel": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/AccessLevel",
                "description": "The explicit access level granted to the owner's team, or `null` when disabled."
              }
            ]
          }
        }
      },
      "StartResponse": {
        "type": "object",
        "description": "Public PKCE challenge and attempt handle; contains no verifier or provider tokens.",
        "required": ["attemptId", "authorizationUrl", "expiresIn"],
        "properties": {
          "attemptId": {
            "type": "string",
            "description": "Opaque owner-bound attempt handle."
          },
          "authorizationUrl": {
            "type": "string",
            "description": "Claude-hosted consent page."
          },
          "expiresIn": {
            "type": "integer",
            "format": "int64",
            "description": "Attempt lifetime in seconds.",
            "minimum": 0
          }
        }
      },
      "StatusResponse": {
        "type": "object",
        "description": "Safe connection metadata.",
        "required": ["enabled", "connected", "ephemeral"],
        "properties": {
          "connected": {
            "type": "boolean",
            "description": "Whether the authenticated Macro user has connected."
          },
          "enabled": {
            "type": "boolean",
            "description": "Whether this deployment supports browser connection."
          },
          "ephemeral": {
            "type": "boolean",
            "description": "Whether reconnecting after service restart is required."
          }
        }
      },
      "UpdateChannelSharePermission": {
        "type": "object",
        "required": ["operation", "channelId"],
        "properties": {
          "accessLevel": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/AccessLevel",
                "description": "The access level for the channel\nThis is required if the operation is add or replace"
              }
            ]
          },
          "channelId": {
            "type": "string",
            "description": "The channel id"
          },
          "operation": {
            "$ref": "#/components/schemas/UpdateOperation",
            "description": "The type of operation to be performed on the chanel share permission\nYou can add, remove or replace and existing permission"
          }
        }
      },
      "UpdateOperation": {
        "type": "string",
        "enum": ["add", "remove", "replace"]
      },
      "UpdateSharePermissionRequestV2": {
        "type": "object",
        "properties": {
          "channelSharePermissions": {
            "type": ["array", "null"],
            "items": {
              "$ref": "#/components/schemas/UpdateChannelSharePermission"
            },
            "description": "Any channel share permissions to be created/updated/removed"
          },
          "linkShare": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/LinkShare",
                "description": "Who can access the item through its share link. Omit to leave unchanged or pass `null` to\ndisable link sharing."
              }
            ]
          },
          "linkShareAccessLevel": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/AccessLevel",
                "description": "The link access level. Omit to leave unchanged or pass `null` to reset it to the default\nlevel when a link share exists."
              }
            ]
          },
          "teamShareAccessLevel": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/AccessLevel",
                "description": "The explicit team access level. Omit to leave unchanged or pass `null` to disable team\nsharing. Only the actual owner may change this setting; `owner` is not an allowed level."
              }
            ]
          }
        }
      },
      "WithAgentSessionId": {
        "type": "object",
        "description": "Just a session id, for the preview variants that carry nothing else.\n\nClients deserialize this, so both derives are used.",
        "required": ["id"],
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid",
            "description": "The session id."
          }
        }
      }
    },
    "securitySchemes": {
      "bearerAuth": {
        "type": "http",
        "scheme": "bearer"
      }
    }
  },
  "tags": [
    {
      "name": "agent-sessions",
      "description": "Agent sessions"
    },
    {
      "name": "agent-models",
      "description": "Fresh provider model discovery"
    },
    {
      "name": "agent-repositories",
      "description": "Repositories a coding session can work on"
    }
  ]
}
