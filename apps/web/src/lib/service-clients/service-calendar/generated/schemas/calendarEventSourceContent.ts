/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * calendar_service
 * OpenAPI spec version: 0.1.0
 */
import type { CalendarEventSourceContentCreatorEmail } from './calendarEventSourceContentCreatorEmail';
import type { CalendarEventSourceContentCreatorName } from './calendarEventSourceContentCreatorName';
import type { CalendarEventSourceContentDescription } from './calendarEventSourceContentDescription';
import type { CalendarEventSourceContentLocation } from './calendarEventSourceContentLocation';
import type { EventReminders } from './eventReminders';
import type { EventTransparency } from './eventTransparency';
import type { EventType } from './eventType';
import type { EventVisibility } from './eventVisibility';

/**
 * The content one provider copy of an event carries.

Google keeps these fields per calendar copy: a shared calendar's copy of a
member's event can have its own title, type, reminders, and access role.
The entity holds its canonical source's values. Every other copy's values
are read from here so a client can show the copy that belongs to the
calendar being viewed.
 */
export interface CalendarEventSourceContent {
  /** Calendar this copy lives on. */
  calendarId: string;
  /** Provider-reported creator email. */
  creatorEmail?: CalendarEventSourceContentCreatorEmail;
  /** Provider-reported creator display name. */
  creatorName?: CalendarEventSourceContentCreatorName;
  /** Optional event body. */
  description?: CalendarEventSourceContentDescription;
  /** Provider event type. */
  eventType: EventType;
  /** Whether the calendar's access role prohibits editing this copy. */
  isReadOnly: boolean;
  /** Optional physical or virtual location label. */
  location?: CalendarEventSourceContentLocation;
  /** Reminder configuration of this copy. */
  reminders: EventReminders;
  /** Display title. */
  title: string;
  /** Availability behavior. */
  transparency: EventTransparency;
  /** Event visibility. */
  visibility: EventVisibility;
}
