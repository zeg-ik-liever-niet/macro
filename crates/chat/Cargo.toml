[package]
edition = "2024"
name = "chat"
publish = false
version = "0.1.0"

[features]
default = ["ai_tools", "inbound", "outbound", "ports"]
ai_tools = ["dep:async-trait", "dep:schemars", "inbound", "ports"]
attachment = [
  "dep:futures",
  "inbound",
]
inbound = [
  "dep:axum",
  "dep:macro_authorization",
  "dep:roles_and_permissions",
  "entity_access/inbound",
  "ports",
]
outbound = [
  "dep:entity_access_db_utils",
  "dep:entity_registry_db_utils",
  "dep:macro_uuid",
  "dep:share_permission_db_utils",
  "dep:sqlx",
  "ports",
]
ports = []

[dependencies]
activity = { path = "../activity", default-features = false }
# Domain (always included)
agent = { path = "../agent" }
ai_toolset = { path = "../ai_toolset" }
anyhow = { workspace = true }
bot_id = { path = "../bot_id" }
attachment = { path = "../attachment" }
chrono = { workspace = true }
entity_access = { path = "../entity_access", default-features = false }
entity_access_db_utils = { path = "../entity_access_db_utils", optional = true }
entity_registry_db_utils = { path = "../entity_registry/entity_registry_db_utils", optional = true }
entity_access_management = { path = "../entity_access_management", default-features = false, features = [
  "ports",
] }
entity_mutation = { path = "../entity_mutation" }
macro_event_broker = { path = "../macro_event_broker", default-features = false }
macro_event_topics = { path = "../macro_event_topics" }
macro_user_id = { path = "../macro_user_id" }
model = { path = "../model" }
non_empty = { path = "../non_empty" }
model-entity = { path = "../model-entity" }
model_owner = { path = "../model_owner" }
models_entity_access_management = { path = "../models_entity_access_management", default-features = false }
models_permissions = { path = "../models_permissions" }
rootcause = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }
unicode-segmentation = { workspace = true }
uuid = { workspace = true }

# Attachment (attachment provider)
futures = { workspace = true, optional = true }

# AI tools (toolset adapter)
async-trait = { workspace = true, optional = true }
schemars = { workspace = true, optional = true }

# Inbound (HTTP handlers)
axum = { workspace = true, optional = true }
macro_authorization = { path = "../macro_authorization", optional = true, default-features = false, features = [
  "axum",
] }
roles_and_permissions = { path = "../roles_and_permissions", optional = true, default-features = false, features = [
  "ports",
] }
utoipa = { workspace = true }

# Outbound (database adapters)
share_permission_db_utils = { path = "../share_permission_db_utils", optional = true }
sqlx = { workspace = true, optional = true }

macro_uuid = { path = "../macro_uuid", optional = true }
workspace-hack = { version = "0.1", path = "../workspace-hack" }

[dev-dependencies]
http-body-util = { workspace = true }
macro_db_migrator = { path = "../macro_db_migrator" }
serde_json = { workspace = true }
tokio = { workspace = true }
tower = { workspace = true }
